<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.czu.claimpaws.notification.persistence.DeliveryMapper">

    <select id="countByEventId" resultType="long">
        SELECT COUNT(1) FROM notification_deliveries WHERE event_id = #{eventId}
    </select>

    <insert id="insert">
        INSERT INTO notification_deliveries (event_id, event_type, payload, signature, status, retry_count, deleted)
        VALUES (#{eventId}, #{eventType}, #{payload}, #{signature}, #{status}, #{retryCount}, FALSE)
    </insert>
</mapper>
