server:
  port: 8080

spring:
  application:
    name: gateway
  config:
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_SERVER_ADDR:127.0.0.1:8848}
    gateway:
      server:
        webflux:
          routes:
            - id: identity
              uri: lb://identity-service
              predicates:
                - Path=/api/v1/auth/**

management:
  endpoints:
    web:
      exposure:
        include: health
